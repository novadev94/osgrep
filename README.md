# mgrep

A command line tool for searching using Mixedbread.

## Usage

```bash
mgrep --api-key <api-key> --store <store-id> <pattern>
mgrep --api-key <api-key> --store <store-id> watch
```

## Development

```bash
pnpm install
pnpm dev
```
